const config = {
  server: {
    url: 'http://localhost:8080',
  },
  rom: {
    path: 'c:\\temp\\hal-roms',
  },
};

export default config;
export const { server } = config;
export const { rom } = config;
